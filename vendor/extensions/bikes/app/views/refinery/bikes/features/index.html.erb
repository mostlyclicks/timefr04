<% content_for :body do %>
  <ul id="features">
    <% @features.each do |feature| %>
      <li>
        <%= link_to feature.title, refinery.bikes_feature_path(feature) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
